# Story 2.2: Soccer Match Discovery and Selection

## Status
Approved

## Story
As the automation system,
I want to identify available soccer matches and their betting options,
so that I can present optimal betting opportunities to the selection algorithm.

## Acceptance Criteria
1. System can navigate to "Today's Matches" section on msport.com
2. All available soccer matches are extracted with team names, start times, and odds
3. For each match, both home/draw/away and over/under 2.5 goals options are captured
4. System filters matches to only include those starting within the next 24 hours
5. Match data includes current odds for all available bet types
6. System handles cases where matches are postponed, cancelled, or odds are suspended
7. Match discovery process completes within 60 seconds for typical daily match volume

## Tasks / Subtasks
- [ ] Extend browser automation for match discovery (AC: 1)
  - [ ] Create navigation service to access "Today's Matches" section
  - [ ] Implement page parsing and data extraction utilities
  - [ ] Add wait conditions for dynamic content loading
  - [ ] Create error handling for navigation failures
- [ ] Implement match data extraction and parsing (AC: 2, 3)
  - [ ] Create match data extraction service with Playwright selectors
  - [ ] Implement parsing for team names, start times, and match details
  - [ ] Add odds extraction for home/draw/away and over/under 2.5 goals
  - [ ] Create data validation and sanitization utilities
- [ ] Build match filtering and validation system (AC: 4, 6)
  - [ ] Implement 24-hour time window filtering logic
  - [ ] Create match status detection (active, postponed, cancelled)
  - [ ] Add odds availability validation
  - [ ] Implement match data quality checks
- [ ] Create comprehensive odds capture system (AC: 5)
  - [ ] Implement odds extraction for all bet types
  - [ ] Add odds timestamp tracking for accuracy
  - [ ] Create odds validation and range checking
  - [ ] Implement odds change detection and logging
- [ ] Implement performance optimization (AC: 7)
  - [ ] Add parallel processing for multiple match pages
  - [ ] Implement caching for repeated data requests
  - [ ] Create performance monitoring and timing
  - [ ] Add timeout handling for slow page loads
- [ ] Build error handling and recovery (AC: 6)
  - [ ] Implement retry logic for failed extractions
  - [ ] Add graceful handling of site maintenance
  - [ ] Create fallback mechanisms for parsing failures
  - [ ] Implement comprehensive error logging
- [ ] Create testing framework for match discovery (AC: 7)
  - [ ] Implement unit tests for data extraction logic
  - [ ] Create integration tests for full discovery flow
  - [ ] Add performance tests to ensure 60-second completion
  - [ ] Implement error scenario testing for robustness

## Dev Notes

### Previous Story Insights
Based on Story 2.1 implementation, we have:
- Browser automation service with Playwright 1.41.0
- Secure session management and login capabilities
- Error handling and recovery mechanisms
- Performance monitoring and logging infrastructure
- Netlify Functions environment for serverless execution
- Comprehensive testing framework for automation components

### Data Models
**Match Discovery Interface** [New for this story]:
```typescript
interface MatchDiscovery {
  id: string;
  sessionId: string;
  userId: string;
  discoveredAt: Date;
  totalMatches: number;
  validMatches: number;
  processingTime: number;
  status: 'in_progress' | 'completed' | 'failed';
  error?: string;
}

interface SoccerMatch {
  id: string;
  msportMatchId: string;
  homeTeam: string;
  awayTeam: string;
  league: string;
  startTime: Date;
  status: 'active' | 'postponed' | 'cancelled' | 'suspended';
  odds: MatchOdds;
  lastUpdated: Date;
}

interface MatchOdds {
  homeWin: number;
  draw: number;
  awayWin: number;
  over25Goals: number;
  under25Goals: number;
  oddsTimestamp: Date;
  isAvailable: boolean;
}

interface MatchDiscoveryResult {
  success: boolean;
  matches: SoccerMatch[];
  totalProcessingTime: number;
  error?: string;
  errorType?: 'navigation_error' | 'parsing_error' | 'timeout' | 'site_maintenance';
}
```

### API Specifications
**Match Discovery Service** [New for this story]:
```typescript
// Netlify Function: /api/automation/discover-matches
interface DiscoverMatchesRequest {
  userId: string;
  sessionId: string;
  timeWindow?: number; // hours, default 24
}

interface DiscoverMatchesResponse {
  success: boolean;
  matches: SoccerMatch[];
  processingTime: number;
  totalMatches: number;
  error?: string;
}
```

### Component Specifications
**Match Discovery Service** [New for this story]:
```typescript
// File: src/services/matchDiscoveryService.ts
class MatchDiscoveryService {
  async discoverMatches(sessionId: string, timeWindow?: number): Promise<MatchDiscoveryResult>
  async extractMatchData(page: Page): Promise<SoccerMatch[]>
  async filterMatchesByTime(matches: SoccerMatch[], hours: number): Promise<SoccerMatch[]>
  async validateMatchData(match: SoccerMatch): Promise<boolean>
  async handleDiscoveryError(error: Error, sessionId: string): Promise<void>
}
```

### File Locations
Based on project structure and tech stack [Source: architecture/tech-stack.md]:
- **Netlify Functions**: `netlify/functions/automation/discover-matches.ts`
- **Discovery Service**: `src/services/matchDiscoveryService.ts`
- **Types**: `src/types/matches.ts`
- **Tests**: `src/__tests__/services/matchDiscoveryService.test.ts`
- **Database Schema**: `supabase/migrations/20240101000004_create_matches_schema.sql`

### Testing Requirements
Based on tech stack [Source: architecture/tech-stack.md]:
- **Framework**: Jest + Playwright for E2E testing
- **Test Location**: `src/__tests__/services/matchDiscoveryService.test.ts`
- **Coverage**: Unit tests for parsing logic, integration tests for discovery flow
- **Performance Testing**: Ensure discovery completes within 60 seconds
- **Error Testing**: Test all error scenarios and recovery mechanisms

### Technical Constraints
Based on architecture documents [Source: architecture/tech-stack.md, architecture/security-and-performance.md]:
- **Performance**: 60-second discovery completion requirement
- **Data Quality**: Comprehensive validation of extracted match data
- **Error Handling**: Robust handling of site changes and maintenance
- **Caching**: Implement caching to avoid repeated requests
- **Logging**: Detailed logging for troubleshooting and audit compliance

### Project Structure Notes
The implementation should follow the established patterns from previous stories:
- Extend the browser automation service from Story 2.1
- Use existing session management and error handling patterns
- Follow established testing patterns with comprehensive coverage
- Integrate with existing logging and monitoring infrastructure
- Maintain security standards for data handling

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2024-12-19 | 1.0 | Initial story creation | Bob (Scrum Master) |

## Dev Agent Record

### Agent Model Used
*To be populated by development agent*

### Debug Log References
*To be populated by development agent*

### Completion Notes List
*To be populated by development agent*

### File List
*To be populated by development agent*

## QA Results
*To be populated by QA agent*
